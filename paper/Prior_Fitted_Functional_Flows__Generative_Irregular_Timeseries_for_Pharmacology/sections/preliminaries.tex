\begin{figure}[h]
    \centering
    \includegraphics[width=\linewidth]{figures/NotationIllustration_FlowMatching.pdf}
    \caption{The model conditions on a population study (green) and dosing schedules (yellow). Based on partial observations (blue) of a new patient, it generates plausible continuations (black) to produce future forecasts (red).
        }
    \label{fig:graphical-model}
\end{figure}


In this section, we introduce the notation and formalize the amortized conditional generation problem we aim to solve. 
%
We then review the optimal-transport (OT) and flow-matching tools we build on, culminating in the conditional OT formulation that underlies our construction.

\subsection{Problem Definition}

We formalize pharmacokinetic (PK) data as sparse, noisy observations of latent, continuous-time concentration functions, and cast inference as learning a conditional probability measure over such functions.

\textbf{Study data}.
%
A pharmacokinetic study consists of noisy concentration measurements collected from a cohort of $I$ individuals $\mathcal{S}=\{\mathcal{D}^1,\dots,\mathcal{D}^I\}$.
%
For individual $i$, we write $\mathcal{D}^{i} = (\textbf{u}^i, \boldsymbol{\tau}^{i}, \mathbf{y}^{i})$, where 
%
$\mathbf{u}^i=(a^i,r^i)$ is the dosing descriptor, with dose amount $a^i\in\mathbb{R}^+$ and administration route $r^i\in\{1,\dots,R\}$.
%
The vector $\boldsymbol{\tau}^{i} = (\tau^{i}_{1}, \dots, \tau^{i}_{T_i})$ contains the (irregular, subject-specific) observation times, and
$\mathbf{y}^{i} = (y^{i}_{1}, \dots, y^{i}_{T_i})$ contains the measured (\textit{e.g.}, plasma) concentrations,
%
with $T_i$ the number of available measurements. 
% Thus, each $\mathcal{D}^{i}$ defines a \textit{concentration trajectory}, and $\mathcal{S}$ a set of trajectories. 
%
Note that the observation times need not be shared across individuals, reflecting the sparse and irregular sampling typical in early-development studies.

\textbf{Latent concentration functions}.
%
Let $\mathcal{T} = [0, T]$ denote the observation window of interest, with horizon time $T$. 
% {\color{red}[ $\mathcal{T}$ is a subintervall? or a set of points? in either case we would need $\subset$ instead of $\in$]}.
%
We model each individual's concentration trajectory as a \textit{latent function} $f: \mathcal{T} \rightarrow \mathbb{R}$, and interpret the measurements $\mathbf{y}^i$ as noisy, irregular samples of $f$ at times $\boldsymbol{\tau}^i$. 
%
To work in a well-defined function space, let $\mathcal{F}$ be a real, separable Hilbert space of functions 
equipped with its Borel $\sigma$-algebra $\mathcal{B}(\mathcal{F})$ and norm $||\cdot\|_\mathcal{F}$.
%
Let us also denote with $\PM(\F)$ the set of Borel probability measures on $\mathcal{F}$.

\textbf{Conditional probability measures}.
%
PK inference is inherently conditional. Predictions for a new individual depend on the study $\mathcal{S}$, dosing information, and possibly a small number of early measurements for that individual. 
%
For now, we collect all such information into a single context variable $c \in \mathscr{C}$, where $\mathscr{C}$ is an abstract context space. 
%
The inferential object of interest is therefore a \textit{conditional probability measure} over concentration functions $\nu(\cdot|c) \in \PM(\F)$. 
%
This measure should capture both the uncertainty due to sparse and noisy observations, and the heterogeneity across individuals implied by the context.

\textbf{The amortized conditional generation problem}.
%
Let $\eta \in \PM(\F)$ be a tractable reference measure over functions (\textit{e.g.}, a Gaussian process).
%
Our goal is to learn a single conditional generator that, given $\eta$ and a context $c$, produces samples distributed according to $\nu(\cdot|c)$.
%
Concretely, we seek a measurable map $T^\star: \mathscr{C} \times \mathcal{F}\rightarrow \mathcal{F}$ such that, for (almost) every context $c \in \mathscr{C}$ we have
%\begin{equation}
$(T^\star(c, \cdot))_{\#} \eta = \nu(\cdot | c).$ %\label{eq:conditional-generation-problem}%\end{equation}
In Section~\ref{sec:conditional-ot}, we introduce conditional optimal transport on product spaces, and the class of triangular maps that formalize the conditional generation problem. %(Eq.~\ref{eq:conditional-generation-problem}).
%
Then, in Section~\ref{sec:model}, we instantiate $c$ to reflect the PK settings of interest --- that is, study-conditioned generation and, for forecasting, conditioning on partial observations of a new patient. 
%

% To formalize PK modeling in the language of generative models, it is natural to work with \emph{distributions over functions}. 
% %
% This viewpoint is motivated by the \emph{continuous-time} nature of PK dynamics, and the fact that clinical measurements are typically \emph{irregularly sampled}: for each individual, the underlying concentration trajectory is a latent function that could, in principle, be queried at any time. 

%  Let $\mathcal{T} \in [0, T]$ be our \textit{input space} (\textit{i.e.}, our observation time window with horizon $T$), with Lebesgue measure $\mu$.
%  %
%  Consider a real, separable Hilbert space $\mathcal{F}$ of functions $f: \mathcal{T} \rightarrow \mathbb{R}$ equipped with the Borel $\sigma$-algebra $\mathcal{B}(\mathcal{F})$, and the inner product $\langle \cdot,\, \cdot \rangle_\mathcal{F}$. 
%  %
%  The prototypical example is $\mathcal{F} = L^2(\mathcal{T},\mu)$, with the usual inner product $\langle f, g\rangle_{L^2(\mathcal{T},\mu)} = \int_\mathcal{T}fg \, d\mu$. 
%  %
%  % [ignore this] {\color{red} this is not a Hilbert space of functions, but of equivalence classes of functions. In particular point evaluations are not well-defined}.  
%  %
%  Let $\PM(\F)$ denote the set of Borel probability measures on  $\F$. 
%  %
%  It will be helpful to specify $\mathcal{F}^u = \mathcal{F} \times \mathbb{R} \times \{0,...,R\}$ the hilbert space that specifies the space where the whole information for one individual lives $(f,\*u) \in \mathcal{F}^u$.

%  \textbf{Space of Conditionals}.
%  %
%  Let $\CM = (\F^u)^I \times \F^u $ be the Hilbert space of individuals timeseries and dosing where the full study and the  past and dosing of the new virtual individual lives in $\C ^n\in \mathscr{C} $. We shift our focus to  measures $\nu \in \PM(\CM \times \mathcal{F})$ that now jointly sample studies and new individuals. In order to solve our problem, we require samples from $\nu(\cdot \mid \C^n)$.  If one has access to an easily to sample measure $\eta_\F \in \PM(\F)$. The objective is to construct a measurable transport map 
% $T^\star_{\mathcal F} : \CM \times \F \to \mathcal F$ 
% such that $T^\star_{\mathcal F}(\mathcal C,\cdot)_{\#}\eta_{\F } = \nu(\cdot \mid \mathcal C^n)$ 
% for every $\mathcal C^n \in \mathcal \CM$. 

% This also constitute a non parametric problem as one should be able to evaluate the function at an arbitrary number of points.  PK datasets are also inherently hierarchical: we observe multiple studies, and within each study multiple individuals. We capture this hierarchy by introducing a meta-distribution $\Omega$ over study-specific measures, $\nu^s \sim \Omega$. Conditional on $\nu^s$, individual trajectories are drawn as functions $f^i \sim \nu^s$, and observations follow $y_j^i = f^i(\tau_j^i) + \epsilon_j^i$, where $\epsilon_j^i$ captures measurement noise and other unmodeled variability. In mixed-effects terminology, study-level structure corresponds to \emph{fixed effects} (indexed by $s$) describing population kinetics, while \emph{random effects} (indexed by $i$) account for individual-specific deviations.

% We now describe the data structure of a single study. Let $\mathcal{S}$ denote one pharmacological study comprising drug concentration--time profiles for $I$ i.i.d.\ individuals, represented as $\mathcal{S}=\{\mathcal{D}^1,\dots,\mathcal{D}^I\}$. For each individual $i$, we define $\mathcal{D}^i=(\mathbf{u}^i,\boldsymbol{\tau}^i,\mathbf{Y}^i,\mathbf{M}^i)$, where $\mathbf{u}^i=(u_1^i,u_2^i)$ encodes dosing information with $u_1^i\in\mathbb{R}$ the dose amount and $u_2^i\in\{1,\dots,R\}$ the dosing route, $\boldsymbol{\tau}^i=(\tau_1^i,\dots,\tau_T^i)$ are (padded) observation times, and $\mathbf{Y}^i=(y_1^i,\dots,y_T^i)$ are the corresponding measured (plasma) concentrations. Here $T$ denotes the maximum number of measurements observed for any individual in the dataset. To accommodate sparsity and irregular sampling, we introduce an observation mask $\mathbf{M}^i=(m_1^i,\dots,m_T^i)$ with $m_j^i\in\{0,1\}$, where $m_j^i=1$ indicates that $y_j^i$ is observed at time $\tau_j^i$ and $m_j^i=0$ otherwise; this construction avoids requiring observation times to be shared across individuals.

% \textbf{PK Inference Problem}.
% %
% Our goal is to amortize inference and generation across studies while formulating tasks at the level of a single study $\mathcal{S}$. We therefore distinguish between the observed individuals $\{\mathcal{D}^i\}_{i=1}^I$ and a new (virtual) individual $\mathcal{D}^{n}$ with $n>I$. For such a virtual individual, we introduce a \emph{measurement index} (split point) $p^n\in\{0,\dots,P\}$, where $P$ is the maximum number of measurements considered for a virtual individual. This index partitions the trajectory into a revealed ``past'' and an unobserved ``future'', and we write this compactly as
% \[
% \mathbf{Y}^n=(\mathbf{Y}_p^n,\mathbf{Y}_f^n),\quad
% \boldsymbol{\tau}^n=(\boldsymbol{\tau}_p^n,\boldsymbol{\tau}_f^n),\quad
% \mathbf{M}^n=(\mathbf{M}_p^n,\mathbf{M}_f^n),
% \]
% where the subscript $p$ denotes the first $p^n$ entries $\*Y_p = (y^i_0,...,y^i_p)$ and $f$ denotes the remaining entries $\*Y^n_f = (y^i_{p+1},...,y^i_T)$.  Informally, $p^n$ specifies how much of the trajectory is revealed as context.

% With this notation, we consider two tasks within a unified conditional generative framework:
% \begin{enumerate}
%     \item \textbf{Population synthesis.}  
%     Without accessing any per-subject information beyond what is contained in $\mathcal{S}$, the goal is to generate 
%     new \textit{virtual} individuals $\{\mathcal{D}^n\}$. That is we would like access to the conditional measure $\nu^s(\cdot|\mathcal{S})$.
%     \item \textbf{Individual prediction.}  
%     Suppose we have access to some observations, we would like the predictive measure defined by:  $\nu^s(\cdot|\*Y^n_p,\boldsymbol{\tau}^n_{p^n},\*M^n_{p^n},\*u^n_{p^n},\mathcal{S})$.
% \end{enumerate}
% In classical time-series generative modeling these are often treated as unconditional versus conditional generation, sometimes requiring separate models \cite{kerrigan2023functional,bilovs2023modeling} or employing conditional guidance at sampling time \cite{kollovieh2024flow,kollovieh2023predict}. In contrast, our approach uses a single conditional measure by encoding the task via $p^n$ and collecting the available information into a context set
% $
% \mathcal{C}^n=\{\mathcal{S},\boldsymbol{\tau}^n_{p^n},\mathbf{u}^n,\mathbf{M}^n,\mathbf{Y}_{p^n}\},
% $
% thereby defining the target conditional distribution
% \begin{equation}
% \nu(\cdot\mid \mathcal{C}^n).
% \end{equation}
% Here $p=0$ entails $\*Y_p = \emptyset$ and corresponds to generation whereas $p \neq 0$ is prediction.


% \subsection{Notation}
%  Let $\mathcal{T} \subseteq \mathbb{R}^d$ be our \textit{input space} (for timeseries this corresponds to the time) and consider a real separable Hilbert space $\mathcal{F}$ of functions $f: \mathcal{T} \rightarrow \mathbb{R}$ equipped with the Borel $\sigma$-algebra $\mathcal{B}(\mathcal{F})$ and the inner product $\langle -,-\rangle_\mathcal{F}$. The prototypical example is $\mathcal{T} \in [0,1]$ equipped with the Lebesque measure and $\mathcal{F} = L^2(\mathcal{T},\mu)$ equipped with is usual inner product $\langle f, g\rangle_{L^2(\mathcal{T},\mu)} = \int_\mathcal{T}fgd\mu$ {\color{red} this is not a Hilbert space of functions, but of equivalence classes of functions. In particular point evaluations are not well-defined}.  In what follows $\PM(\F)$ is the set of Borel probability measures on  $\F$. It will be helpful to specify $\mathcal{F}^u = \mathcal{F} \times \mathbb{R} \times \{0,...,R\}$ the hilbert space that specifies the space where the whole information for one individual lives $(f,\*u) \in \mathcal{F}^u$.
\begin{figure*}[t]
    \centering
    \includegraphics[width=0.8\linewidth]{figures/NetworkArchitectureSketch-FlowMatching.pdf}
    \caption{
    Top left: study context consists of time series of different individuals. Bottom left: data for a target individual are split into past observations $(\tau^{\tt P},y^{\tt P})$ and future observations $(\tau^{\tt F},y_1^{\tt F})$. With a uniformly distributed interpolation parameter $t$, $y_t^{\tt F}$ is obtained by interpolating between observed data $y_1^{\tt F}$ and a sample $y_0^{\tt F}$ from a Gaussian  conditional path. Right: model architecture consisting of a study context encoder (top) and decoder (bottom). Abbreviations: $\tau$: study time; $y$ observed plasma concentration; $r$: route of administration; $a$: dosing amount; $\Phi$, multilayer perceptron; $\mathfrak{F}$: Fourier features; $\psi$/$\Psi$: self-/cross-attention; Q/K/V/M/L: query/key/value/block diagonal mask/layers.
%    First we encode $\mathcal{S}$ with a self attention that respects the permutational invariance of the representation through masking. We interpolate between $\*x_{\tt F}$ and  $y_{\tt F}$ to obtain $y^{\tt F}_t$. representations at both the individual and study levels. Our transformer-based decoder embeds the encoder representations alongside dose information. Finally, we define functional queries that allow us to evaluate the predictive distribution at any target time $\tau$. By introducing $\mathbf{z}_i$ and $\mathbf{z}_s$, we can model fixed and random effects, enabling a population-aware, individualized characterization of dynamics.
}
    \label{fig:architecture}
\end{figure*}

\subsection{OT and Flow Matching}

Our model builds on a standard link between optimal transport (OT) and flow-based generative modeling. 
%
%OT provides a principled way to move probability mass from a simple \textit{source} distribution to a complex \textit{target} distribution, while flow models realize this movement via an ODE driven by a learned velocity field~\citep{lipman2022flow}. 
%
%The Benamou–Brenier formulation makes this connection explicit by characterizing OT as the minimum kinetic energy needed to transport one distribution into another over continuous time.

\textbf{Unconditional OT}.
%
Let $\eta,\nu\in\PM(\F)$ be probability measures on the (separable Hilbert) function space $\F$. 
%
In the Monge formulation, OT seeks a measurable transport map $T:\F\to\F$ that pushes $\eta$ to $\nu$ (\textit{i.e}, $T_{\#}\eta=\nu$) while minimizing squared displacements. 
%
This yields the squared 2-Wasserstein distance
\begin{equation}
W_2^2(\eta,\nu) = \inf_{T:\F\to\F} \int_{\F} \|f - T(f)\|_{\F}^2\, d\eta(f),
\label{eq:w2-functional}
\end{equation}
where the infimum is taken over all $T$ which fulfill $T_{\#}\eta=\nu$.

%
The same quantity admits the dynamical Benamou-Brenier  formulation~\citep{benamou2000computational}:
\begin{equation}
W_2^2(\eta,\nu) = \min_{(\gamma_t,v_t)} \int_0^1\int_{\F} \|v_t(f)\|_{\F}^2\, d\gamma_t(f)\, dt,
\label{eq:benamou_brenier}
\end{equation}
subject to the boundary conditions $\gamma_0=\eta$, $\gamma_1=\nu$, and the continuity equation
\begin{equation}
\partial_t\gamma_t+\mathrm{div}(v_t\gamma_t)=0.
\label{eq:continuity}
\end{equation}
Here $(\gamma_t)_{t\in[0,1]}\subset\PM(\F)$ is a time-indexed path of measures, and $v_t:\F\to\F$ is a time-dependent \textit{vector field}.
%
Intuitively, $v_t$ specifies how ``particles'' $f$ move through $\F$, so that their resulting path measure $(\gamma_t)_{t\in[0,1]}$ evolves from $\eta$ to $\nu$. Thus the transport map $T$ is parameterized through the vector field $v_t$.
%
% This is precisely the viewpoint adopted by flow-based generative models. Draw $f(0) \sim \eta$ and evolve $f(t)$ according to $\dot f (t) = v_t(f(t))$ to obtain $f(1) \sim \nu$.

 The above two formulations are also known as static and dynamic optimal transport, respectively. In practice, directly solving the Benamou–Brenier minimization problem (Eq. \ref{eq:benamou_brenier}) is intractable, as it requires optimizing over the space of all possible measure paths and vector fields. Flow Matching circumvents this by using the fact that any vector field satisfying the continuity equation (Eq. \ref{eq:continuity}) generates a valid transport (though not ad hoc one of minimal costs).  More specifically, Flow matching replaces direct OT optimization with a supervised regression problem, in which one (i) defines a family of simple conditional interpolation paths between paired samples $f_0 \sim \eta$ and $f_1 \sim \nu$, 
%
(ii) derives the corresponding conditional velocity field explicitly, and 
%
(iii) learns a parametric model $v^\theta_t(\cdot)$ to match this vector field.
%
While in (i) any coupling $\rho$ works, choosing an OT-aligned coupling (e.g., via mini-batch OT) is crucial; it ensures the interpolation paths are straight, which minimizes the kinetic energy and aligns the Flow Matching objective with the Benamou–Brenier objective ~\citep{pooladian2023multisample, tong2023improving}. We explain the functional version of Flow Matching in more detail in the following section.
%
%We also note conditional extensions developed in subsequent work~\citep{chemseddine2025conditional}.

\textbf{Functional Flow Matching.}~\citep{kerrigan2023functional}. 
%
Let $f_0\sim\eta$ and $f_1\sim\nu$, and let $z=(f_0,f_1)\sim\rho$ for some coupling $\rho\in\Pi(\eta,\nu)$ (later to be chosen as minibatch-OT). 
%
To address step (i) from the previous section, Functional Flow Matching defines a Gaussian conditional path $\gamma_t(\cdot| z)$, which interpolates between source and target samples: \begin{equation}
\gamma_t(\,\cdot \mid z) = \mathcal{N}\!\bigl((1-t) f_0 + t f_1,\; \Sigma\bigr), 
\label{eq:fm-cond-path}
\end{equation}
where $\Sigma$ is a trace-class covariance operator on $\F$ \citep{da2014stochastic}.
%
This conditional path has the advantage that it allows us to derive the associated conditional vector field $v_t(\cdot| z)$ corresponding to step (ii) explicitly:
\begin{equation}
v_t(\cdot \mid z) = f_1 - f_0.
\label{eq:fm-cond-vf}
\end{equation}
%
While these conditional objects describe the trajectory of individual sample pairs, the marginal path $\gamma_t$ describes the evolution of the full probability density from $\eta$ to $\nu$. It is obtained by mixing the conditional path over the coupling,
\begin{equation}
\gamma_t =\int \gamma_t(\,\cdot \mid z)\, d\rho(z),
\label{eq:fm-marg-path}
\end{equation}
and the corresponding marginal vector field can be written as
\begin{equation}
v_t(\cdot)
=
\int v_t(\cdot \mid z)\,
\frac{d\gamma_t(\,\cdot \mid z)}{d\gamma_t}\,
d\rho(z).
\label{eq:fm-marg-vf}
\end{equation}
Crucially, the goal is to learn this marginal field $v_t$, which drives the global transport.
To realize step (iii), one learns a parametric vector field $v^\theta_t(f)$ by solving the regression problem for the conditional vector field (Eq.~\ref{eq:fm-cond-vf})
\begin{equation*}
\mathcal{L}(\theta)=\mathbb{E}_{t\sim\mathcal{U}[0,1]}\,
\mathbb{E}_{z\sim\rho}\,
\mathbb{E}_{f\sim\gamma_t(\cdot\mid z)}
\bigl[\|v^\theta_t(f)- (f_1-f_0)\|_{\F}^2\bigr].
\label{eq:fm-loss}
\end{equation*}
Theoretical results guarantee that minimizing this objective has the same gradients as the (intractable) mean-squared error to the marginal field
 $v_t$ (Eq.~\ref{eq:fm-marg-vf}) \citep{tong2023improving}. 
%
% Choosing $\rho$ as an OT (mini-batch) coupling between $\eta$ and $\nu$ recovers OT-aligned couplings that commonly improve training stability and sampling efficiency.


% \subsection{Flow Matching and Benamou-Brenier}

% We recall the quadratic-cost optimal transport problem and its dynamical characterization, which clarifies the connection to flow-based generative modeling. 
% %
% In the Monge formulation, the squared $2$-Wasserstein distance between probability measures $\eta$ and $\nu$ on a function space $\mathcal F$ is
% \begin{equation}
%   W_2^2(\eta,\nu)
%   \;=\;
%   \inf_{T}  \int_{\mathcal F\times\mathcal F} \|g-T(g)\|_{\mathcal F}^2\,d\eta(g)
%   \label{eq:w2-functional}
% \end{equation}
% subject to the pushforward constraint $T\#\eta=\nu$. The same quantity admits the Benamou--Brenier dynamical formulation \cite{benamou2000computational}:
% \begin{equation}
% \label{eq:benamou_brenier}
% W_2^2(\eta,\nu)
% =
% \min_{(\gamma_t,v_t)}
% \int_0^1 \int_{\mathcal T}
% \lvert v_t(\tau) \rvert^2 \, d\gamma_t(\tau)\, dt
% \end{equation}
% \begin{equation}
% \text{s.t.}\quad
% \gamma_0 = \eta,\qquad
% \gamma_1 = \nu,\qquad
% \partial_t \gamma_t + \operatorname{div}(v_t \gamma_t) = 0 .
% \label{eq:continuity}
% \end{equation}
% In generative modeling, one typically chooses a tractable source $\eta$ and a target $\nu$ given via samples; learning a transport map (or, equivalently, a velocity field) then enables sampling from $\nu$ by pushing forward samples from $\eta$ along the induced flow. Mini-batch OT couplings provide a practical mechanism to reduce variance and straighten paths in conditional flow matching \cite{pooladian2023multisample,tong2023improving}, with further connections between conditional flow matching objectives and OT established in subsequent work \cite{chemseddine2025conditional}.

% \paragraph{Functional Flow Matching.}
% We adopt conditional flow matching \cite{lipman2022flow} in function spaces \cite{kerrigan2023functional}. Let $f_0\sim\eta$ and $f_1\sim\nu$, and let $z=(f_0,f_1)\sim\rho\in\Pi(\eta,\nu)$ be any coupling. Define conditional path measures and their conditional vector fields by
% \begin{align}
% \gamma_t(\,\cdot \mid z)
% &= \mathcal{N}\!\bigl(t f_1 + (1-t) f_0,\; C\bigr), \label{eq:fm-cond-path}\\
% v_t(\,\cdot \mid z)
% &= f_1 - f_0, \label{eq:fm-cond-vf}
% \end{align}
% where $C$ is a trace-class covariance operator \citep{da2014stochastic}. The marginal path and induced marginal vector field are obtained by mixing over the coupling:
% \begin{align}
% \gamma_t
% &= \int \gamma_t(\,\cdot \mid z)\,\mathrm{d}\rho(z), \label{eq:fm-marg-path}\\
% v_t
% &= \int v_t(\,\cdot \mid z)\,
% \frac{\mathrm{d}\gamma_t(\,\cdot \mid z)}{\mathrm{d}\gamma_t}\,
% \mathrm{d}\rho(z). \label{eq:fm-marg-vf}
% \end{align}
% We learn a parametric field $v^\theta(t,f)$ via the conditional regression objective
% \begin{equation}
% \mathcal{L}(\theta)
% =
% \mathbb{E}_{t\sim\mathcal{U}[0,1]}
% \mathbb{E}_{z\sim\rho}
% \mathbb{E}_{f\sim\gamma_t(\cdot\mid z)}
% \bigl[\|v^\theta(t,f)-v_t(f\mid z)\|^2\bigr],
% \label{eq:fm-loss}
% \end{equation}
% whose gradient matches that of the MSE to the (intractable) marginal field $v_t$ \citep{tong2023improving}. Selecting $\rho$ as (mini-batch) OT between $\eta$ and $\nu$ recovers the OT-aligned couplings commonly used to improve training and sampling efficiency.

\subsection{Conditional Optimal Transport in Function Space}
\label{sec:conditional-ot}
The previous section described Flow Matching in the \textit{unconditional} setting. However, our problem is inherently conditional: we observe a context $c\in \CM$ (e.g., patient covariates, dosing regimen) and seek to sample from a target conditional measure $\nu(\cdot\mid c)\in\PM(\F)$. 
%
Rather than solving a separate generative problem for every possible context, we formalize this as Conditional Optimal Transport. We seek a single \textit{triangular} transport map on the product space $\CM\times\F$ that generalizes across contexts, effectively amortizing the inference. As a warning, it is important to distinguish the notion of conditioning used in Flow Matching (conditioning on the path endpoints $z=(f_0, f_1)$ to define the vector field) from the conditioning on an external context $c \in \mathscr{C}$ (e.g., patient covariates).

%\subsection{Conditional Optimal Transport in Function Space}
%\label{sec:conditional-ot}

%The previous section described Flow Matching in the \textit{unconditional} setting.
%
%Now, we turn to the conditional problem, in which we observe a context $c \in \mathscr{C}$ and seek to sample from a (target) conditional measure $\nu(\cdot\mid c) \in \PM(\F)$.
%
%Conditional optimal transport formalizes this by learning a single transport map on the product space $\mathscr{C} \times \F$ that generalizes across contexts.

\paragraph{Conditional OT.}
Let $\mu \in \PM(\mathscr{C})$ denote a measure over contexts. We consider joint measures on $\mathscr{C} \times \F$ whose marginal on $\mathscr{C}$ equals $\mu$,
\begin{equation}
\PM_{\mu}(\mathscr{C} \times \F)
=
\Bigl\{\pi \in \PM(\mathscr{C} \times \F) : \pi_{\mathscr{C}} = \mu \Bigr\},
\label{eq:Pmu_def}
\end{equation}
where $\pi_{\mathscr{C}}$ denotes the $\mathscr{C}$-marginal of $\pi$. We take as reference joint measure the product $\eta
=
\mu \otimes \eta_{\F},$ where $\eta_{\F} \in \PM(\F)$ is an easy-to-sample base measure over functions. A target joint measure $\nu \in \PM_{\mu}(\mathscr{C} \times \F)$ is characterized by its conditional distributions $\nu(\cdot\mid c)$.

For conditional OT, we seek \textit{triangular} transport maps $T: \mathscr{C} \times \F \to \mathscr{C} \times \F$.
%
These maps leave the context component unchanged, formally defined as:
\begin{equation}
T(c,f) = \bigl(c,\, T_{\F}(c,f)\bigr),
\label{eq:triangular_map}
\end{equation}
where $T_{\F}: \mathscr{C} \times \F \to \F$.
%
If $T$ is triangular and satisfies the global pushforward constraint $T_{\#} \eta = \nu$, then the \textit{fiberwise pushforward} identity holds for $\mu$-almost every context $c \in \mathscr{C}$:
\begin{equation}
\bigl(T_{\F}(c,\cdot)\bigr)_{\#} \eta_{\F}
=
\nu(\cdot \mid c).
\label{eq:fiberwise_pushforward}
\end{equation}
This formulation effectively amortizes the transport problem: learning a single global map $T$ solves the conditional generation problem for all contexts simultaneously.

\paragraph{Triangular Functional Flows.}
In the dynamic Benamou--Brenier framework (Eq.~\ref{eq:benamou_brenier}), a triangular transport map corresponds to a flow generated by a \textit{triangular vector field} $v_t: \mathscr{C} \times \mathcal{F} \to \mathscr{C} \times \mathcal{F}$.
%
The context $c$ is static , i.e. it represents fixed attributes that do not evolve during the generative process, which implies that its time derivative must be zero. This imposes a block structure (triangularity) on the vector field:
\begin{equation}
v_t(c,f) = \begin{pmatrix} 0 \\ v_t^\mathcal{F}(c,f) \end{pmatrix},
\label{eq:triangular_vf}
\end{equation}
where $v_t^\mathcal{F}: \mathscr{C} \times \mathcal{F} \to \mathcal{F}$ drives the evolution of the function $f$ conditioned on $c$.
%
Consequently, the learning problem reduces to estimating the functional component $v_t^\mathcal{F}$. We achieve this by parameterizing $v_t^\mathcal{F}$ with a neural network and minimizing the Flow Matching objective (Eq.~\ref{eq:fm-loss}), treating $c$ as an additional conditioning input to the network.

%\paragraph{Conditional OT via simulation-free flows.}
%Let $\mu\in\PM(\CM)$ denote a measure over contexts. We consider joint measures on $\CM\times\F$ whose marginal on $\CM$ equals $\mu$,
%\begin{equation}
%\PM_{\mu}(\CM\times\F)
%=
%\Bigl\{\pi\in\PM(\CM\times\F)\,:\,\pi_{\CM}=\mu\Bigr\},
%\label{eq:Pmu_def}
%\end{equation}
%where $\pi_{\CM}$ denotes the $\CM$-marginal of $\pi$. We take as reference joint measure the product
%\begin{equation}
%\eta
%=
%\mu\otimes\eta_{\F},
%\label{eq:eta_product}
%\end{equation}
%where $\eta_{\F}\in\PM(\F)$ is an easy-to-sample base measure over functions. A target joint measure $\nu\in\PM_{\mu}(\CM\times\F)$ is characterized by its conditional distributions $\nu(\cdot\mid c)$. For conditional OT, we seek (block) triangular transport maps  $T:\CM\times\F\to\CM\times\F$. Those triangular transport maps  leave the context component unchanged, \textit{i.e.},
%\begin{equation*}
%T(c,f)=\bigl(c,\,T_{\F}(c,f)\bigr)
%\quad
%\text{for some} \, \, T_{\F}:\CM\times\F\to\F.
%\label{eq:triangular_map}
%\end{equation*}
%If $T$ is triangular and satisfies $T_{\#}\eta=\nu$, then the fiberwise pushforward identity\footnote{By fiberwise we mean ``for each fixed content''. In other words, for a given $c$ we restrict to the slice $\{c\} \times \mathcal{F}$ and consider the map $T_{\mathcal{F}}(c, \cdot): \mathcal{F} \rightarrow \mathcal{F}$.} holds for all context  $c\in\CM$, except on a set of $\mu$-measure zero
%\begin{equation}
%\bigl(T_{\F}(c,\cdot)\bigr)_{\#}\eta_{\F}
%=
%\nu(\cdot\mid c).
%\label{eq:fiberwise_pushforward}
%\end{equation}
% That is, the context is left unchanged and only the functional component is being transported. This is reflected in the fact that, similar to Eq. \ref{eq:w2-functional} and Eq. \ref{eq:benamou_brenier}, the triangular transport stays in correspondence to a triangular velocity field $v_t: \mathcal{C} \times \mathcal{F} \to \mathcal{C} \times \mathcal{F}$, where triangularity means that $v_t(c,f) = \begin{pmatrix} 0 & v_t^\mathcal{F}(c,f) \end{pmatrix}$, where $v_t^\mathcal{F}:\mathcal{C} \times \mathcal{F} \to \mathcal{F}$. This triangular velocity field is what we parameterize by a network and learn by minimizing the flow matching loss.

%\begin{definition}[Triangular Maps]
%A measurable map $T:\CM\times\F\to\CM\times\F$ is (block) triangular if it leaves the context component unchanged, \textit{i.e.},
%\begin{equation*}
%T(c,f)=\bigl(c,\,T_{\F}(c,f)\bigr)
%\quad
%\text{for some} \, \, T_{\F}:\CM\times\F\to\F.
%\label{eq:triangular_map}
%\end{equation*}
%\end{definition}
%If $T$ is triangular and satisfies $T_{\#}\eta=\nu$, then the fiberwise pushforward identity\footnote{By fiberwise we mean ``for each fixed content''. In other words, for a given $c$ we restrict to the slice $\{c\} \times \mathcal{F}$ and consider the map $T_{\mathcal{F}}(c, \cdot): \mathcal{F} \rightarrow \mathcal{F}$.} holds for all context  $c\in\CM$, except on a set of $\mu$-measure zero
%\begin{equation}
%\bigl(T_{\F}(c,\cdot)\bigr)_{\#}\eta_{\F}
%=
%\nu(\cdot\mid c).
%\label{eq:fiberwise_pushforward}
%\end{equation}
%Thus, triangularity encodes the conditional structure. That is, the context is left unchanged and only the functional component is being transported.

%\begin{definition}[Conditional Monge Problem]
%Given $\eta,\nu\in\PM_{\mu}(\CM\times\F)$ and a cost function
%$c:(\CM\times\F)\times(\CM\times\F)\to\mathbb{R}$,
%the conditional Monge problem seeks a triangular transport map $T^{\star}$ solving
%\begin{equation}
%\begin{aligned}
%\inf_{T}\quad
%&\int_{\CM\times\F}
%c\bigl((c,f),\,T(c,f)\bigr)\,d\eta(c,f)\\
%\text{s.t.}\quad
%&\begin{cases}
%T(c,f)=\bigl(c,\,T_{\F}(c,f)\bigr),\\
%T_{\#}\eta=\nu.
%\end{cases}
%\end{aligned}
%\label{eq:conditional_monge}
%\end{equation}
%\end{definition}
%The dynamical characterization of this conditional problem --- and the corresponding conditional Benamou--Brenier viewpoint developed by \citet{kerrigan2024dynamic} --- will be the key tool we exploit in the next section to design conditional flow-matching objectives in function space.


% \subsection{Conditional Optimal Transport in Function Space}
% On the previous section, we introduced the relationship between flow matching and optimal transport in the context of \textit{unconditional generation}. Our methodology  however, relies heavily on \textit{conditional generation} as amortization is attained through conditioning on $\mathcal{S}$. Moreover, for prediction, one is further required to condition on the past $\mathcal{D}^n_p$. Conditional transport techniques \cite{hosseini2025conditional} have been developed  that allows for sampling from conditional distributions and has been widely used in the context of Bayesian inverse problems. In this section we present the main results from conditional optimal transport required for our task.

% Let $\CM = (\F^u)^I \times \F^u $ be the Hilbert space of individuals timeseries and dosing where the full study and the  past and dosing of the new virtual individual lives in $\C ^n\in \mathscr{C} $. We shift our focus to  measures $\nu \in \PM(\CM \times \mathcal{F})$ that now jointly sample studies and new individuals. In order to solve our problem, we require samples from $\nu(\cdot \mid \C^n)$.  If one has access to an easily to sample measure $\eta_\F \in \PM(\F)$. The objective is to construct a measurable transport map 
% $T^\star_{\mathcal F} : \CM \times \F \to \mathcal F$ 
% such that $T^\star_{\mathcal F}(\mathcal C,\cdot)_{\#}\eta_{\F } = \nu(\cdot \mid \mathcal C^n)$ 
% for every $\mathcal C^n \in \mathcal \CM$. 

% The direct approach would be to train one such map (generative model) per study i.e. one such generative model per conditional. This is of course both computationally expensive and impossible since in our application each study has very few individuals to be able to  train a generative model of its own. Instead, one seeks to leverage shared structure across different conditioning by learning a single conditional transport map that generalizes across $\mathcal C^n$. This idea underlies recent work on conditional transport; in particular, 
% \cite{hosseini2025conditional} introduce triangular transport maps to enable such amortization.

% \begin{definition}[Triangular Maps] \footnote{For our application, we take a restricted formulation of triangular maps than the one suggested in \cite{hosseini2025conditional} as the \textit{conditioning} component can be a }
% A map $T: \CM \times \F \rightarrow \CM \times \F$ is (block) triangular if there exist a map  $T_\F: \CM \times \F \rightarrow \F$ the $\F$-component) so that $T(\C^n,f) = (\C^n,T_\F(\C^n,f))$ for all $(\C^n,f) \in \CM \times \F$. 
% \end{definition}

% This map is important since as if $T$ is triangular and $T\# \eta = \nu$ its $\F$-component $T_\F$ solves the problem. We will take $\eta = \nu_\CM  \otimes \eta_\F$ \cite{kerrigan2024dynamic}. Placing $\nu_\CM$ the marginal over the conditioning variable $\CM$. Now to introduce the machinery that  relates transports with distances on probability spaces we introduce the Conditional Monge Problem:

% \begin{definition}[Conditional Monge]\label{def:conditional_monge} Given source and target measures  $\eta,\nu \in \PM^{\nu_\mathcal C}( \CM \times \F)$ reference $\eta = \nu_{\CM} \otimes \eta_{\mathcal F}$ and a cost function $c : (\CM \times \F) \times (\CM \times \F) \to \mathbb R$. Find $T^\star$ that solves:
% \begin{equation}
% \label{eq:conditional_monge_function_space}
% \begin{aligned}
% \inf_{T}\; &\int_{\mathcal \CM \times \F}
% c\bigl(\mathcal C,v;T(\mathcal C,f)\bigr)\, d\eta(\mathcal C,f),\\
% \text{s.t.}\quad
% &\begin{cases}
% T(\C,f)=\bigl(\C, T_{\mathcal F}(\C,f)\bigr),\\
% T_{\#}\eta = \nu.
% \end{cases}
% \end{aligned}
% \end{equation}
% \end{definition} 

% Here, the transport map is constrained to be \emph{triangular} in the sense that
% its $\mathcal \CM$--component is fixed to be the identity map.
% This reflects the fact that the reference measure $\eta$ and the target measure
% $\nu$ share the same $\CM$--marginal, and therefore no mass needs to be
% transported in the $\CM$--direction. Since the transport map is triangular, the $\mathcal F$--component
% $T_{\mathcal F}^\star$ solves the conditional Monge problem fiberwise, that is,
% for $\nu_{\mathcal S}$--almost every $\mathcal C \in \mathcal S$,
% \[
% T_{\mathcal F}^\star(\mathcal C,\cdot)_\# \eta_{\F}
% =
% \nu(\cdot \mid \mathcal C),
% \]
% where $\nu(\cdot \mid \mathcal C)$ denotes the conditional distribution of functions given
% $\mathcal C$. The dynamical formulation of the Conditional Monge Problem  as well as the Conditional version of the Benau Bermier theorem presented in \cite{kerrigan2024dynamic} is what we will exploit in the next section in order to develop a functional flow matching scheme that allows for amortization.